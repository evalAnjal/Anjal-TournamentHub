getting started with our app

getting started with our app

first clone from github using git clone https://github.com/evalAnjal/Anjal-TournamentHub.git

then run the command `npm install`

then run `npm run dev`

first clone from github using git
